apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: c-ambassador-job
  namespace: c-bes
  annotations:
    flux.weave.works/automated: "false"
spec:
  releaseName: c-ambassador-job
  interval: 1m
  chart:
   spec:
    chart: platforms/hyperledger-besu/charts/besu-tlscert-cert-gen
    sourceRef:
      kind: GitRepository
      name: flux-skk
      namespace: flux-skk
  values:
    metadata:
      name: c-ambassador-job
      namespace: c-bes
    image:
      alpineutils: ghcr.io/hyperledger/bevel-alpine:latest
      pullSecret: regcred
      pullPolicy: IfNotPresent
    network:
      tmtls: True
    node:
      name: c
      clientport: 8888
    vault:
      address: http://demo.sau99.dev.aws.blockchaincloudpoc-develop.com
      secretengine: secretsv2
      authpath: besuc
      rootcasecret: data/c-bes/crypto/ambassadorcerts/rootca
      ambassadortlssecret: data/c-bes/crypto/c/tls
      role: vault-role
      serviceaccountname: vault-auth
      type: hashicorp
    subject:
      ambassadortls: CN=DLT Root CA/OU=DLT/O=DLT/L=London/C=GB
    opensslVars:
      domain_name_pub: c.sau99.dev.aws.blockchaincloudpoc-develop.com
      domain_name_priv: c.c-bes
      domain_name_tessera: c-tessera.c-bes
    healthcheck:
      retries: 10
      sleepTimeAfterError: 2

